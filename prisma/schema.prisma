// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

//////////////////////////////////
//         USER MODEL           //
//////////////////////////////////
model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String   // Hashed password
  role      Role     @default(USER)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  devices   Device[]
}

enum Role {
  USER
  ADMIN
}


//////////////////////////////////
//         DEVICE MODEL         //
//////////////////////////////////
model Device {
  id          Int      @id @default(autoincrement())
  serial      String   @unique // Unique identifier for the device
  name        String
  location    String?  // Optional location string
  ownerId     Int      // Foreign key linking to the User who registered it
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  lastSeen    DateTime?

  // Relationship: Each device must be owned by one User
  owner       User     @relation(fields: [ownerId], references: [id])
    
  //telemetry   Telemetry[]
  //alerts      Alert[]
  //tokens      DeviceRegistrationToken[]

  @@index([ownerId]) // Indexing the foreign key for faster queries
}

// model Telemetry {
//   id          Int      @id @default(autoincrement())
//   deviceId    Int
//   ts          DateTime @default(now())
//   payload     Json
//   temperature Float?
//   humidity    Float?
//   battery     Float?
//   device     Device   @relation(fields: [deviceId], references: [id])
//   @@index([deviceId, ts])
// }

// model DeviceRegistrationToken {
//   id        Int      @id @default(autoincrement())
//   deviceId  Int
//   token     String   @unique
//   expiresAt DateTime
//   device    Device   @relation(fields: [deviceId], references: [id])
// }


/* model Device {
  id          String   @id @default(uuid())
  userId      String
  name        String
  description String?
  createdAt   DateTime @default(now())
  lastSeen    DateTime?
  user        User     @relation(fields: [userId], references: [id])
  telemetry   Telemetry[]
  alerts      Alert[]
  tokens      DeviceRegistrationToken[]
}


model Alert {
  id        String   @id @default(uuid())
  deviceId  String
  ts        DateTime @default(now())
  type      String
  payload   Json
  device    Device   @relation(fields: [deviceId], references: [id])
}
*/